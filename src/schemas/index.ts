import {
    movieSchema,
    createMovieSchema,
    updateMovieSchema,
    returnAllMoviesSchema
} from "./movies.schemas";
export { movieSchema, createMovieSchema, updateMovieSchema, returnAllMoviesSchema };
